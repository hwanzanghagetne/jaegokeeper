<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
          http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
          http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 메일 관련 빈 스캔: GmailMailService, MailTemplateConfig 등 -->
    <context:component-scan base-package="com.jaegokeeper.common.mail"/>
    <context:annotation-config/>

    <!-- mail.properties 로딩 -->
    <context:property-placeholder location="classpath:mail.properties"/>

    <!-- MailTemplateConfig만 등록 -->
    <bean class="com.jaegokeeper.config.MailTemplateConfig"/>

    <!-- JavaMailSender (root-context에 있던 것 그대로 복사) -->
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="${mail.host}"/>
        <property name="port" value="${mail.port}"/>
        <property name="username" value="${mail.username}"/>
        <property name="password" value="${mail.password}"/>

        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">${mail.smtp.auth}</prop>
                <prop key="mail.smtp.starttls.enable">${mail.smtp.starttls.enable}</prop>
                <prop key="mail.smtp.starttls.required">${mail.smtp.starttls.required}</prop>
                <prop key="mail.smtp.connectiontimeout">${mail.smtp.connectiontimeout}</prop>
                <prop key="mail.smtp.timeout">${mail.smtp.timeout}</prop>
                <prop key="mail.smtp.writetimeout">${mail.smtp.writetimeout}</prop>
            </props>
        </property>
    </bean>

</beans>

<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xsi:schemaLocation="
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- CORS 등 이 컨텍스트에서 사용하는 ${...} 환경변수 플레이스홀더 해석용 -->
    <context:property-placeholder/>

    <!-- 어노테이션 기반 MVC 활성화(UTF-8 설정 포함) -->
    <annotation-driven>
        <!-- SecurityConfig는 부모(root) 컨텍스트에 있고 MVC는 이 자식 컨텍스트에 있으므로,
             @AuthenticationPrincipal 처리기를 MVC 쪽에 명시적으로 등록한다. -->
        <argument-resolvers>
            <beans:bean class="org.springframework.security.web.method.annotation.AuthenticationPrincipalArgumentResolver"/>
        </argument-resolvers>
        <message-converters>
            <!-- byte[] 응답(springdoc의 /v3/api-docs 등)이 Jackson에 의해 base64 문자열로
                 감싸이지 않도록 Jackson보다 먼저 등록 -->
            <beans:bean class="org.springframework.http.converter.ByteArrayHttpMessageConverter"/>
            <beans:bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <beans:constructor-arg value="UTF-8"/>
            </beans:bean>
    <!--json 날짜 포맷-->
            <beans:bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter">
                <beans:property name="objectMapper" ref="objectMapper"/>
            </beans:bean>
        </message-converters>
    </annotation-driven>

    <!-- springdoc이 이름으로 찾는 conversion service (annotation-driven 내부용과 별개로 명시 등록) -->
    <beans:bean id="mvcConversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean"/>

    <!-- CORS는 root-context.xml의 CorsFilter 빈으로 처리
         (mvc:cors 네임스페이스는 allowed-origins의 ${...} 플레이스홀더를 치환하지 않는 문제 확인) -->

    <!-- 정적 리소스 처리 -->
    <resources mapping="/resources/**" location="/resources/" />

    <!-- Swagger UI 리소스 (springdoc). webjar 내부 경로가 버전 하위 폴더라 버전 고정 매핑
         (swagger-ui 버전 올릴 때 이 경로도 같이 갱신 필요).
         swagger-override를 먼저 찾아서, webjar 기본값(데모 Petstore API 주소)이 박힌
         swagger-initializer.js 대신 우리 /v3/api-docs를 가리키는 버전으로 덮어씀 -->
    <resources mapping="/swagger-ui/**"
               location="classpath:/swagger-override/,classpath:/META-INF/resources/webjars/swagger-ui/5.32.2/" />
    <resources mapping="/webjars/**" location="classpath:/META-INF/resources/webjars/" />

    <!-- springdoc-openapi 2.x (non-Boot이라 자동설정이 없어 수동 등록) -->
    <beans:bean class="org.springdoc.core.properties.SpringDocConfigProperties"/>
    <beans:bean class="org.springdoc.core.properties.SwaggerUiConfigProperties"/>
    <beans:bean class="org.springdoc.core.properties.SwaggerUiOAuthProperties"/>
    <beans:bean class="org.springdoc.core.configuration.SpringDocConfiguration"/>
    <beans:bean class="org.springdoc.webmvc.core.configuration.SpringDocWebMvcConfiguration"/>
    <beans:bean class="org.springdoc.webmvc.ui.SwaggerConfig"/>
    <beans:bean class="org.springframework.boot.autoconfigure.web.WebProperties"/>
    <beans:bean class="org.springframework.boot.autoconfigure.web.servlet.WebMvcProperties"/>

    <!-- 컴포넌트 스캔: 컨트롤러 계층만 -->
    <context:component-scan base-package="com.jaegokeeper" use-default-filters="false">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>


    <!-- JSP ViewResolver 설정 -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="viewClass" value="org.springframework.web.servlet.view.JstlView" />
        <beans:property name="prefix" value="/WEB-INF/views/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>

    <beans:bean id="jsonView" class="org.springframework.web.servlet.view.json.MappingJackson2JsonView">
        <beans:property name="contentType" value="application/json;charset=UTF-8"/>
    </beans:bean>

    <!-- servlet multipartResolver 설정 -->
    <beans:bean id="multipartResolver"
          class="org.springframework.web.multipart.support.StandardServletMultipartResolver">
    </beans:bean>

    <!-- 인증(로그인 세션 확인)은 Spring Security의 SecurityFilterChain(web.xml의
         springSecurityFilterChain)이 담당한다. servlet-context.xml 레벨의 인터셉터는 더 이상 없음. -->

</beans:beans>

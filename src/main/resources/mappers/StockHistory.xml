<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jaegokeeper.hwan.mapper.StockHistoryMapper">

<!--    재고조정시 히스토리 삽입-->
    <insert id="insertHistory">
        INSERT INTO stock_history (stock_id, his_type, quantity, created_at)
        VALUES (#{stockId}, #{hisType}, #{delta}, NOW())
    </insert>

</mapper>